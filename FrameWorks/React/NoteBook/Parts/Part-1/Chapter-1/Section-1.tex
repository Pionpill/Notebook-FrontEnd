\part{基础知识}
\chapter{创建第一个项目}
\section{安装与配置}

\textit{React 官网提供了网页开发环境\footnote{\url{https://codepen.io/gaearon/pen/oWWQNa?editors=0010}}，这里讲的是在 Vscode 这类 IDE 环境下搭建开发环境，如果是在网页开发，可以跳过这一节内容。}

\textit{主要参考文献：
\begin{itemize}
    \item 入门教程: 认识 React: \url{https://react.docschina.org/tutorial/tutorial.html}
    \item 创建新的 React 应用: \url{https://react.docschina.org/docs/create-a-new-react-app.html}
\end{itemize}}






\subsection{准备工作}
\subsubsection{前置知识}

React 作为一个前端开发框架，需要开发者具备以下基础知识:

\begin{itemize}
    \item HTML5,CSS3。
    \item JavaScript 以及 ES6 的一些新特性，包括: 箭头函数，\texttt{class，let，const} 语句。
    \item 基础的 bash 指令(我的操作系统下是 powershell)。
    \item Node.js 和 npm 基础指令(可以边学 React 边了解)。
\end{itemize}

\subsubsection{环境准备}

确保机器上安装过以下环境：
\begin{itemize}
    \item Node.js >= 14.0.0
    \item npm >= 5.6
\end{itemize}

npm 包含在 Node.js 中，如果没有安装 Node，可以参考这篇文章\footnote{\url{https://blog.csdn.net/antma/article/details/86104068}}。如果已安装，可通过以下指令查看 Node.js 和 npm 版本。

\begin{bash}
node -v
npm -v
\end{bash}

\subsection{React 配置}
\subsubsection{创建项目(下载 React 包)}

在准备完成后，执行如下操作:
\begin{enumerate}
    \item 在终端 \texttt{cd} 到需要创建项目的文件夹下。
    \item 执行如下指令(其中，my-app 为应用名，可以更改):
\begin{bash}
npx create-react-app my-app
\end{bash}
    如果是第一次创建，需要等待一段时间下载 react 包。这个过程中极有可能因为 \texttt{react} 在外网的问题，下载失败，可以通过切换镜像解决:
\begin{bash}
npm config set registry https://registry.npm.taobao.org
\end{bash}
    \item 下载完成后，会自动创建一个界面，进入 my-app 项目。
\begin{bash}
cd my-app
\end{bash}
    \item 启动项目。
\begin{bash}
npm start
\end{bash}
\end{enumerate}

至此，我们就成功创建并启动了一个 React 项目。在进行下一节操作前，我们要删除 src/ 文件夹下的所有子文件(src 目录不要删) 来创建我们的项目。可以通过图形化界面操作，也可以通过命令行。

\begin{bash}
cd src
del *
cd ..
\end{bash}